{% extends 'base.html.twig' %}

{% block title %}Edit Galerie{% endblock %}

{% block main %}
    <h1>Edit Galerie</h1>

    {{ include('galerie/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_galerie_index') }}">back to list</a>
    {% if is_granted('ROLE_ADMIN') %}
        {{ include('galerie/_delete_form.html.twig') }}
    {% endif %}
{% endblock %} {#main#}
